[build]
  command = "npm run build"
  functions = "functions"
  publish = "build"

  [dev]
    framework = "#custom"
    command = "npm run dev"
    targetPort = 8080
    port = 8888
    publish = "build"
    autoLaunch = false